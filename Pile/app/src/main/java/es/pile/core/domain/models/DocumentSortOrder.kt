package es.pile.core.domain.models

/**
 * Determines the ordering used when displaying the document list.
 */
enum class DocumentSortOrder {
    /** Most recently added documents first. */
    NEWEST,

    /** Oldest documents first. */
    OLDEST
}
