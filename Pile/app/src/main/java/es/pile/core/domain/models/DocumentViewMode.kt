package es.pile.core.domain.models

/**
 * Layout used to display the documents in the main screen.
 */
enum class DocumentViewMode {
    /** Vertical rows with a small cover, name, date and size. */
    LIST,

    /** Grid of big cover thumbnails ("icon view"). */
    GRID
}
